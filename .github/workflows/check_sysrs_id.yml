name: Check Commit Messages

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  check_commits:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Get PR commits
        id: commits
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          PR_NUMBER=$(jq -r .pull_request.number "$GITHUB_EVENT_PATH")
          gh api repos/${{ github.repository }}/pulls/$PR_NUMBER/commits \
            --jq '.[].commit.message' > commit_messages.txt
          echo "Found commits:"; cat commit_messages.txt

      - name: Check for forbidden strings and create warning message
        id: check
        run: |
          lines=()

          if grep -qi "WIP" commit_messages.txt; then
            lines+=("- [ ] ⚠️ **WIP**: コミットメッセージに 'WIP' が含まれています。作業途中のままではありませんか？")
          fi
          if grep -qi "FIXME" commit_messages.txt; then
            lines+=("- [ ] ⚠️ **FIXME**: 'FIXME' が含まれています。修正漏れがないか確認してください。")
          fi
          if grep -qi "temp" commit_messages.txt; then
            lines+=("- [ ] ⚠️ **temp**: 'temp' が含まれています。一時的なコードやメッセージを削除してください。")
          fi

          if [ ${#lines[@]} -gt 0 ]; then
            {
              echo "found=true"
              echo "warnings<<'EOF'"
              echo "### 🚨 コミットメッセージ警告チェックリスト"
              printf '%s\n' "${lines[@]}"
              echo "EOF"
            } >> "$GITHUB_OUTPUT"
          else
            echo "found=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Comment on PR if needed
        if: steps.check.outputs.found == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          PR_NUMBER=$(jq -r .pull_request.number "$GITHUB_EVENT_PATH")
          # --raw-field を使うと本文がそのまま送られ、改行も保持されます
          gh api repos/${{ github.repository }}/issues/$PR_NUMBER/comments \
            --method POST \
            --raw-field body="${{ steps.check.outputs.warnings }}"
